namespace Unity.Simulator
{
    // Not sure why the Simulator is so broken right now...
    public static class SimulatorFix
    {
        public static bool IsIOS = false;
        public static bool IsAndroid = false;
    }
}